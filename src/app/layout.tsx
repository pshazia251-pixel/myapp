import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import Header from "@/components/Header";
import Footer from "@/components/Footer";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: {
    default: "BillFreeUSA - Save Money on Your Monthly Bills",
    template: "%s | BillFreeUSA",
  },
  description:
    "BillFreeUSA helps Americans reduce and eliminate monthly bills. Expert tips on saving money, cutting costs, and achieving financial freedom.",
  keywords: [
    "bill free",
    "save money",
    "reduce bills",
    "financial freedom",
    "USA",
    "cut costs",
    "budgeting",
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">
        <Header />
        <main className="flex-1">{children}</main>
        <Footer />
      </body>
    </html>
  );
}
